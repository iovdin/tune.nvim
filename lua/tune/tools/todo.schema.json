{
  "description": "Update a todo list file with status changes from given text",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Text containing 'status - task' lines to update in the todo list"
      },
      "filename": {
        "type": "string",
        "description": "Filename of the todo list file to read and write"
      }
    },
    "required": ["text", "filename"]
  }
}
